type ConsultDetailsLowerProps = {
  value: string | null; // TODO: add all fields
  onChange: (value: string | null) => void;
};

export const ConsultDetailsLower = ({
  value,
  onChange,
}: ConsultDetailsLowerProps) => {
  return (
    <>
      <h1>bottom text</h1>
    </>
  );
};
